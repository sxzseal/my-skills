{
  "role": "shared-primitive",
  "description": "Add or extend a shared UI primitive in src/features/_shared/. Only invoked when a primitive is needed by 2+ features.",
  "readOnly": false,
  "allowedTools": ["Read", "Glob", "Grep", "Write", "Edit", "Bash"],
  "allowedBashPatterns": [
    "^npm (run|list) ",
    "^pnpm (run|list) ",
    "^npx tsc",
    "^git (status|diff|log|show) ",
    "^ls ",
    "^node scripts/lib/forge-(state|events|patch|budget)\\.mjs "
  ],
  "deniedBashPatterns": [
    "\\bgit push\\b",
    "\\brm -rf\\b"
  ],
  "requiredOutputs": [
    { "path": ".loop/dev/subagent-receipts/<id>.json", "schema": "subagent-receipt" }
  ],
  "promptInjections": [
    "You may only add to src/features/_shared/**; never touch feature-specific files.",
    "The primitive must be truly generic (no hard-coded feature data or domain terms)."
  ]
}
